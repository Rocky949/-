 截至11月19日前的实验作业

1、验证文字常量区
	（1）char *p = "abcd"; char *q="abcd"; p和q是不是指向同一个位置
	（2）char [] str ="abcd"; 与p和q不同

2、验证内存对齐的方案
（1）课件中 CIC和CCI 两个内存大小是否相同
	（2）pack()的使用，改变对齐方式

3、验证局部性
	（1）寄存器变量的使用，会加快程序的运行效率
（2）寄存器的使用是建议性的
	（3）Cache效果的验证（矩阵存储与读取过程对比）

4、验证重载=的赋值效果
（1）Object a=b; 和a =c 分别调用拷贝构造 和 重载=的操作
（2）返回值类型是引用(&)要比反回值的效率高
（3）参数传引用和传值会效率上会有差别

以上作业，需要使用C++来进行。

int fib2(int n)
{
	if(n==0)
		return 0;
	if(n==1)
		return 1;
	return fib2(n-1)+fib2(n-2);
}
